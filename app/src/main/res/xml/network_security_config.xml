<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted ONLY for local development hosts so the app can
  reach the Rime voice proxy at http://10.0.2.2:3000 (Android emulator's alias
  for the host machine's localhost). All other traffic must be HTTPS. This is
  intentionally narrow: no global usesCleartextTraffic.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
